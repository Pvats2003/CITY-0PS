{
  "name": "city-ops-functions",
  "private": true,
  "description": "City Ops OS — Firebase Cloud Functions (server-side only). Not part of the Vite client bundle; never included in `npm run build` at the repo root.",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
